import { HoverPropagationDirective } from './hover-propagation.directive';

describe('HoverPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
